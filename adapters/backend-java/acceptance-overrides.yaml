# backend-java acceptance overrides

phase_overrides:
  code-quality:
    lint:
      run: "mvn checkstyle:check"
    typecheck:
      run: "mvn compile"
    test:
      run: "mvn test"
  e2e:
    e2e-code:
      run: "mvn verify"
  build:
    build:
      run: "mvn package -DskipTests"
